# 应用边界

`apps/` 将可直接运行的两端明确隔离：

- `web/`：React 单页应用。只处理界面、表单状态、路由和 API 调用，不保存密钥，也不直接访问数据库或上传文件目录。
- `api/`：FastAPI 服务。负责身份认证、角色权限、项目数据隔离、文件归一化、多模态检索、报告生成和运营接口。

本地开发时 Vite 仅把 `/api`、`/health` 代理到 `api`；生产环境由 Nginx 提供相同的反向代理规则，因此前端不需要写死服务地址。
