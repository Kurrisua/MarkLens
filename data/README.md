# 数据目录规则

- `samples/`：可提交 Git 的最小、脱敏、许可明确样本；
- `raw/`：原始数据，只存本地或受控存储，已被 `.gitignore` 排除；
- `processed/`：规范化和派生数据，已排除；
- `indexes/`：向量或搜索索引，已排除。

每个真实数据集必须有单独的数据卡，记录来源、许可、字段、获取日期、版本、限制和已知偏差。当前仓库未包含真实商标数据。
